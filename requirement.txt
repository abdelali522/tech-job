# --- Core Data Engineering ---
pyspark>=3.5.0          # PySpark (Compatible up to 4.x depending on your local config)
kafka-python>=2.0.2     # Kafka Producer for sending messages to topics

# --- Data Ingestion (APIs & Scraping) ---
requests>=2.31.0        # For handling API calls in api_ingestion.py
beautifulsoup4>=4.12.3  # For parsing HTML in scraper.py
urllib3>=2.0.7          # HTTP client required by requests

# --- Orchestration ---
apache-airflow>=2.8.0   # For scheduling and managing the pipeline

# --- Configuration & Utilities ---
python-dotenv>=1.0.1    # For loading database credentials in config/settings.py
pandas>=2.2.0           # For lightweight tabular transformations (if used before Spark)

# --- Database (Optional for Python, Spark uses the JAR) ---
psycopg2-binary>=2.9.9  # Standard PostgreSQL adapter for Python